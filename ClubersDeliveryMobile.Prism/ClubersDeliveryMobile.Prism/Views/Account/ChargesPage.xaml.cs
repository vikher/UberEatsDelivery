using Xamarin.Forms;

namespace ClubersDeliveryMobile.Prism.Views
{
    public partial class ChargesPage : ContentPage
    {
        public ChargesPage()
        {
            InitializeComponent();
        }

        /*private void OnClic(object sender, System.EventArgs e)
        {
            first.BackgroundColor = (Color)Application.Current.Resources["cluberspink"];
            second.BackgroundColor = Color.Gray;
            third.BackgroundColor = Color.Gray;
        }

        private void OnClik(object sender, System.EventArgs e)
        {
            first.BackgroundColor = Color.Gray;
            second.BackgroundColor = (Color)Application.Current.Resources["cluberspink"];
            third.BackgroundColor = Color.Gray;
        }

        private void OnClick(object sender, System.EventArgs e)
        {
            first.BackgroundColor = Color.Gray;
            second.BackgroundColor = Color.Gray;
            third.BackgroundColor = (Color)Application.Current.Resources["cluberspink"];
        }*/
    }
}
